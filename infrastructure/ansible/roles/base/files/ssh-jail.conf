[sshd]
enabled  = true
bantime  = 600
findtime = 30
maxretry = 5
port     = {{ ssh_port }},ssh
ignoreip = {{ admin_lan.cidr }},{{ mgmt_lan.cidr }},{{ kube_hosts.cidr }},{{ wireguard.cidr }},{{ tailscale.cidr }}